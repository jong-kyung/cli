{
  "scripts": {
    "deploy": "<%= packageManager %> run build && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/vite-plugin": "^1.26.0"
  },
  "devDependencies": {
    "wrangler": "^4.70.0"
  }
}
